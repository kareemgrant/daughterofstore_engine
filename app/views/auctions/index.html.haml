%h1 Current Auctions

%table
  %tr
    %th
    %th
    %th

  - @auctions.each do |auction|
    - product = auction.product
    %tr
      %td= image_tag product.photo.url
      %td= link_to "#{product.title}", auction
      %td= link_to "#{product.description[0..29]}...", auction
%br

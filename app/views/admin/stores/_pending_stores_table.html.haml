.row-fluid
  .span12
    %table.table.table-striped.table-bordered.cart-detail
      %thead
        %tr
          %th.span2 Store Name
          %th.span2 Store Path
          %th.span4 Description
          %th.span4 Actions
      %tbody
        - stores.each do |store|
          %tr
            %td= store.name
            %td= store.path
            %td= truncate(store.description.capitalize, length: 32)
            %td
              - if store.status == "pending"
                = button_to "Approve", admin_store_path(store, toggle: "approve"), method: :put, class: "btn btn-link" 
                |
                = button_to "Decline", admin_store_path(store, toggle: "decline"), method: :put, class: "btn btn-link" 
              - else
                = link_to "Approve", admin_store_path(store, toggle: "approve"), :method => :put

